# MasterRepo Phase 12.1 Season Config Patch Pack

This patch updates the seasonal system to match the locked Master Repo rule:

- seasons are configurable in both client and server settings
- seasons are usually enforced server-side
- default target season length is about 6 months

## Purpose
Replace the earlier short seasonal assumption with a configurable, server-authoritative season model.
