# Release Operations

Required release outputs:
- packaged client build
- packaged dedicated server build when applicable
- build metadata
- content manifest
- release notes
- logs
- optional symbols package

Minimum release gates:
- compile succeeds
- validation succeeds
- smoke tests succeed
- packaging succeeds
